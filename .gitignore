# This repo has no build step and no dependencies — see CLAUDE.md.
# node_modules is listed only to catch an accidental `npm install`.
/node_modules/

# Local tooling / editor scratch
.playwright-mcp/
*.log
.DS_Store
Thumbs.db

# Local test credentials (even public-scoped ws_live_ keys stay out of git —
# this repo has shipped committed keys twice; see CLAUDE.md rule 3)
.env
