# This repo has no build step and no dependencies — see CLAUDE.md.
# node_modules is listed only to catch an accidental `npm install`.
/node_modules/

# Local tooling / editor scratch
.playwright-mcp/
*.log
.DS_Store
Thumbs.db

# Personal working notes kept beside the repo, not part of its record.
/my_notes.md

# Local test credentials (even public-scoped ws_live_ keys stay out of git —
# this repo has shipped committed keys twice; see CLAUDE.md rule 3)
.env

# Scratch host page for hitting a real backend: it carries a live ws_live_ key in
# its script tag. demo/index.html is the committed one and runs on data-mock.
demo/demo.html
